const jest = require("jest");

test('creates an employment test', () => {


    expect(Employee).toBe('');
    expect(Engineer).toEqual('');
    expect(Intern).toEqual('');
    expect(Manager).toEqual('');
});